#include "main.h"

/**
 * more_numbers - main function
 * Retun: 0
 */

void more_numbers(void)
{
	char i, j;
	int k = 0;

	while (k < 10)
	{
		for (i = 0; i <= 14; i++)
		{
			i = j;
			if (i > 9)
			{
				putchar(1);
				j = i % 10;
			}
			_putchar('0' + p);
		}
		_putchar('\n');
		k++;
	}
}
