#include "main.h"

/**
 * print_square - main function
 * @size: parameter
 * Return:0
 */

void print_square(int size)
{
	int i = 0, j;

	while (i < size && n > 0)
	{
		size = 0;
		while (size < n)
		{
			_putchar('#');
		}
		_putchar('\n');
		i++;
	}
	if (i == 0)
	_putchar('\n');
}
