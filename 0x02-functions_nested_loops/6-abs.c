#include "main.h"
/**
 * _abs -main function
 * @i: parameter
 * Description: prints the absolute value of an integer
 * Return: abs of a number
 */

int _abs(int i)
{
		if (i < 0)
	{
		i = -i;
	}
	return (i);
}
