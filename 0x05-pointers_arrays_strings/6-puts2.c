#include "main.h"
#include <stdio.h>

/**
 * puts2 - main function
 * @str: parameter
 * Rueturn:0
 */

void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if ((i % 2) == 0)

		else
			continue;
	}
	_putchar('\n');
}
